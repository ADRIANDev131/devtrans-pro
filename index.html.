<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Adrián Tech | Traductor Pro</title>
    <style>
        /* Estilos integrados para que Acode no falle */
        body { background-color: #0f172a; color: white; font-family: sans-serif; margin: 0; padding: 0; }
        header { background-color: #1e293b; padding: 20px; text-align: center; border-bottom: 2px solid #3b82f6; }
        .container { padding: 20px; max-width: 600px; margin: auto; }
        textarea { width: 100%; height: 150px; background-color: #1e293b; color: white; border: 1px solid #475569; border-radius: 10px; padding: 10px; box-sizing: border-box; }
        button { width: 100%; background-color: #2563eb; color: white; border: none; padding: 15px; border-radius: 10px; font-weight: bold; margin-top: 10px; cursor: pointer; }
        button:hover { background-color: #1d4ed8; }
        .glosario { margin-top: 30px; background-color: #1e293b; padding: 15px; border-radius: 10px; }
        h2 { color: #60a5fa; }
    </style>
</head>
<body>

    <header>
        <h1>ADRIÁN <span style="color:#60a5fa text-transform: uppercase;">Tech</span></h1>
    </header>

    <div class="container">
        <h2>Traductor de IA</h2>
        <textarea id="entrada" placeholder="Escribe aquí para probar..."></textarea>
        <button onclick="probar()">PROBAR TRADUCTOR</button>

        <div class="glosario">
            <h3>Glosario para Monetizar</h3>
            <p><strong>Hosting:</strong> Donde vive tu web.</p>
            <p><strong>SEO:</strong> Para aparecer en Google.</p>
        </div>
    </div>

    <script>
        function probar() {
            const texto = document.getElementById('entrada').value;
            if(texto == "") {
                alert("Escribe algo en Acode para ver que funciona");
            } else {
                alert("¡Funciona! Texto recibido: " + texto);
            }
        }
    </script>
</body>
</html>
